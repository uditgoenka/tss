## TSS Terminal Rules

Use `tss run -- <command>` for noisy terminal commands and `tss raw <id>` when a lossy summary omits details.

Treat package-manager and environment commands as raw unless TSS classifies the exact command shape as optimized.
