## TSS Terminal Rules

Prefer `tss run -- <command>` for noisy terminal commands.

Use `tss proxy <command>` when exact passthrough is required.

When output contains `tss raw <id>`, inspect the raw output before relying on missing lines.
